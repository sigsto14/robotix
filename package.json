{
	"name": "robot",
	"version": "1.0.0",
	"author": "Sigrid",
	"description": "It's time we took control of the robots",
	"scripts": {
		"start": "npx ts-node index.ts",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"dependencies": {
		"@types/node": "^22.14.0"
	}
}